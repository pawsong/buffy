const PREFIX = 'console';

export const MASTER_INITIAL_TAB = `${PREFIX}.master.initial.tab`;

export const MASTER_PANE_V_SIZE = `${PREFIX}.master.pane.vertical.size`;

export const MASTER_PANE_LEFT_H_SIZE = `${PREFIX}.master.pane.left.horizontal.size`;
