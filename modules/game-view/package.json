{
  "name": "@pasta/game-view",
  "version": "1.0.0",
  "description": "- Listens to state store event - Handle UI input",
  "main": "src/index.js",
  "built:main": "lib/index.js",
  "scripts": {
    "clean": "../../node_modules/.bin/rimraf lib",
    "test": "echo \"Error: no test specified\" && exit 1",
    "link": "node ../../tools/link",
    "prepublish": "npm run link && npm run build",
    "build": "npm run clean && npm run build:lib",
    "build:lib": "../../node_modules/.bin/babel src --out-dir lib"
  },
  "license": "ISC",
  "dependencies": {
    "eventemitter3": "^1.1.1",
    "keyboardjs": "^2.2.0",
    "shader-particle-engine": "^1.0.2",
    "three": "^0.73.0"
  },
  "peerDependencies": {
    "material-ui": "^0.13.3",
    "react": "^0.14.3",
    "react-dom": "^0.14.3",
    "redux": "^3.0.4",
    "bluebird": "^3.0.5"
  }
}
