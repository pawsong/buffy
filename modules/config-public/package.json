{
  "name": "@pasta/config-public",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "prepublish": "npm run link && npm run build",
    "link": "node ../../tools/link",
    "clean": "../../node_modules/.bin/rimraf lib",
    "build": "npm run clean && ../../node_modules/.bin/tsc",
    "build:watch": "npm run clean && ../../node_modules/.bin/tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:watch": "../../node_modules/.bin/tsc --watch"
  },
  "author": "",
  "license": "ISC",
  "link": [
    "../config-internal"
  ]
}
